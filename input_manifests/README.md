# Pasta de manifests

Esta pasta existe apenas como ponto de organizacao local.

Neste projeto, os manifests oficiais continuam sendo lidos a partir da run externa configurada em `source_experiment_dir`.

Nao copie nem edite os manifests de referencia aqui se o objetivo for preservar comparabilidade metodologica.
